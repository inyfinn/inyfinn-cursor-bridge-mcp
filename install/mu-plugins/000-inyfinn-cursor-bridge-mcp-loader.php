<?php
/**
 * Legacy stub — NIE ładuje wtyczki.
 *
 * Od 1.6.0 Inyfinn Cursor Bridge MCP startuje wyłącznie po przycisku Włącz
 * (lista active_plugins). Ten plik zostawiony w paczce tylko po to, żeby
 * ręczna kopia z install/mu-plugins/ nie omijała aktywacji.
 *
 * Jeśli leży w wp-content/mu-plugins/, Installer::remove_mu_plugin_loader()
 * kasuje go przy Włącz / Wyłącz / plugins_loaded.
 *
 * @package Inyfinn_Cursor_Bridge_MCP
 */

defined( 'ABSPATH' ) || exit;

return;
